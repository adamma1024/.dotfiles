- defaults:
    link:
      relink: true
      create: true

- clean: ["~"]

- link:
    ~/.bashrc: bashrc
    ~/.gitconfig: gitconfig

    ~/.config/nvim: nvim

    ~/.zshrc: zshrc

- shell:
    - [git submodule update --init --depth 1 --recursive, Installing submodules]
